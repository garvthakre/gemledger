import React from 'react'

function PolishingDashboard({ user }) {
    return (
      <div>
        <h2>Polishing Dashboard</h2>
        <p>Update polished diamonds and transfer ownership.</p>
        <button>Transfer Ownership</button>
      </div>
    );
  }

export default PolishingDashboard